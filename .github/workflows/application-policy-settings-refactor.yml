name: APPLICATION policy-settings one-time refactor

on:
  push:
    branches:
      - refactor/application-policy-settings

permissions:
  contents: write

jobs:
  refactor:
    if: ${{ !contains(github.event.head_commit.message, '[policy-settings-refactor-applied]') }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Apply extraction
        run: node scripts/refactor-application-policy-settings.mjs

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Commit refactor
        run: |
          rm scripts/refactor-application-policy-settings.mjs
          rm .github/workflows/application-policy-settings-refactor.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Extract APPLICATION policy settings [policy-settings-refactor-applied]"
          git push origin HEAD:refactor/application-policy-settings
